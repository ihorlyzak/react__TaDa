import { Box } from '@mui/system';
import styled from 'styled-components';

export const AccountActionsBox = styled(Box)({
  display: 'flex',
  alignItems: 'center',
  gap: '10px',
  marginTop: '10px',
});
